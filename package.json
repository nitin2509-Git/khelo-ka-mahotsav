{
  "name": "khelo-os-sprint-1",
  "version": "0.1.0",
  "private": true,
  "description": "Sprint 1 MVP for Khelo OS tournament operations.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check tests/app.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
